Go inside folder: `cd folderName`

Go Back one folder: `cd ..`

(32, 44) always enter a space after a comma.

switch():   enter a space before opening a '{' bracket.

To run a program: ` node fileName `.

 To link a js file at html  <!-- <script src="object.js" </script>  -->

 when you give a name for file dont give a space.

let x = "64";
let y = 8;
console.log(x/y);  // When we divide a string and number the result is 8.
console.log(x*y);  // When we multiply a string and number the result is 512.
console.log(x-y);  // When we subtract a string and number the result is 56.
console.log(x+y);  // When we add a string and number the result is 648.

let x="50"
let y=10
console.log(+x+y); // When we a

