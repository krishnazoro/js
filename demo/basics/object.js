let person = {
    name:'krish',
    age:22,
    color:'brown'
}

let reminder = 'study';
console.log(person);

console.log(person.name);

console.log(person, reminder);

person.game = 'cricket';
console.log(person);

delete person.color;
console.log(person, reminder);

