const message = 'This is a message';
const merge = message.split('  ');
console.log(merge);

const combined = merge.join('-');
console.log(combined);

const numbers = [1, 2, 3];
const combine = numbers.join(',');
console.log(combine);

