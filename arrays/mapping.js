const numbers = [1, 2, -3, 3, 4, 5];
// 
const items = numbers
    .filter(n => n >= 0)
    .map(n => n * n );
// 
// console.log(items);

// multiply by 2

// const numbers = [1, 2, 3, 4, 5, 6];
// 
// const multiply = numbers.map((numbers) => numbers * 2);
// 
// console.log(multiply);